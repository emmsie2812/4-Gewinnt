#include "Screen.hpp"
#include <iostream>

int main(){
    //Screen screen = new Screen();
    Screen screen;
    //screen->startMenu();     //Open start Menu, start game in there
    screen.startMenu();     //Open start Menu, start game in there

    return 0;
}